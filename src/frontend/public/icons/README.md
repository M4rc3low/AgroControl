# AgroControl application icons

`icon-512.png` is the canonical raster source for installable clients. Tauri native icon assets are generated from it with `npm run desktop:icons` before desktop builds. Keep the source PNG valid and do not replace generated native formats manually.
