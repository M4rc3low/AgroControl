name: Generate Sprint 5 Migration

on:
  push:
    branches:
      - "feat/sprint-5-machinery-market"
    paths:
      - "src/backend/AgroControl.Domain/Modules/Machinery/**"
      - "src/backend/AgroControl.Domain/Modules/Market/**"
      - "src/backend/AgroControl.Infrastructure/Persistence/AgroControlDbContext.cs"
      - ".github/workflows/generate-sprint5-migration.yml"

permissions:
  contents: write

jobs:
  generate:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Setup .NET
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: "10.0.x"

      - name: Restore
        run: dotnet restore AgroControl.sln

      - name: Install EF CLI
        run: dotnet tool install --global dotnet-ef --version 10.0.0

      - name: Generate migration
        run: >-
          dotnet ef migrations add MachineryMarketCore
          --project src/backend/AgroControl.Infrastructure/AgroControl.Infrastructure.csproj
          --startup-project src/backend/AgroControl.Infrastructure/AgroControl.Infrastructure.csproj
          --output-dir Persistence/Migrations

      - name: Commit generated migration
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add src/backend/AgroControl.Infrastructure/Persistence/Migrations
          git diff --cached --quiet && exit 0
          git commit -m "db: add MachineryMarketCore migration"
          git push
