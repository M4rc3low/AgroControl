apiVersion: 1

providers:
  - name: AgroControl
    orgId: 1
    folder: AgroControl
    type: file
    disableDeletion: false
    updateIntervalSeconds: 30
    options:
      path: /var/lib/grafana/dashboards
